import 'package:flutter/material.dart';

const whiteColorStyle = TextStyle(color: Colors.white);
const circularBorder = BorderRadius.all(Radius.circular(25));
const movieBorder = BorderRadius.all(Radius.circular(10));
const headerStyle = TextStyle(
  fontSize: 25,
  fontWeight: FontWeight.bold,
  color: Colors.white,
);
const movieDetailPadding = EdgeInsets.only(
  top: 20,
  left: 20,
  right: 20,
);
const movieDetailContainerRadius = Radius.circular(24);
const movieDetailHeaderStyle = TextStyle(fontSize: 22, fontWeight: FontWeight.bold);
const detailText = TextStyle(fontSize: 18);